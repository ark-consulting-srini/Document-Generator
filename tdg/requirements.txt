# Core
streamlit>=1.28.0
pandas>=1.5.0
openpyxl>=3.1.0,<4.0.0     # cap to avoid version conflicts with pandas in Databricks Runtime

# Databricks
databricks-sdk>=0.20.0
requests>=2.28.0

# Visualization (Data Model tab, Task DAG)
# Note: also requires the graphviz *system binary* (apt-get install graphviz).
# In Databricks Apps, add an init script or use the Databricks cluster init:
#   %sh apt-get install -y graphviz
graphviz>=0.20.0

# Document generation (Word export)
python-docx>=0.8.11

# SQL parsing (for .sql file lineage extraction)
sqlglot>=11.0.0

# Anthropic Claude API (optional — for LLM-powered TDD/Conversion Report)
# In Databricks Apps, set ANTHROPIC_API_KEY via a Databricks Secret Scope
# (see app.yaml for instructions). For local dev, use .streamlit/secrets.toml.
anthropic>=0.40.0
